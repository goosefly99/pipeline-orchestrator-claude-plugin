{
  "name": "pipeline-orchestrator-auto-dev",
  "description": "Pipeline orchestrator MCP server \u2014 reads pipeline.toml, resolves the typed DAG, validates artifacts, manages run state, and coordinates research/debate/synth phases",
  "version": "1.0.1",
  "author": {
    "name": "olive"
  }
}
